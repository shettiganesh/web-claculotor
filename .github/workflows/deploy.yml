name: Web Calculator

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Show Files
        run: |
          echo "Web Calculator Application"
          pwd
          ls -la

      - name: Run Application Check
        run: |
          echo "Application checked successfully"

      - name: Success
        run: echo "GitHub Actions completed successfully!"
